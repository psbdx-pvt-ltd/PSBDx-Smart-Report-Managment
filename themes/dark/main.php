<?php
/**
 * Theme Name: Dark
 * Description: A sleek dark-mode look for the report form, its fields, and the ticket response/view page.
 * Version: 1.0.0
 * Author: PSBDx
 * Requires SRM: 1.4.7
 *
 * This file is intentionally just a header — PSBDX_SRM_Themes reads the
 * comment block above via get_file_data() and never executes this file's
 * PHP. The actual look lives in the sibling style.css, scoped under the
 * `.psbdx-theme-dark` wrapper class so it can never bleed into forms that
 * use a different theme on the same page.
 *
 * @package PSBDx_Smart_Report_Management
 */

// Exit if accessed directly.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
